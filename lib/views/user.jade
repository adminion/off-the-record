extends layout

block scripts
  script(src='/socket.io/socket.io.js')
  script(src='/scripts/off-the-record.js')
  script(src='/scripts/user.js')
block styles
  link(ref='/styles/user.css')

block pageTitle
    #pageTitle=request.requestedUser.username

block content
  #contents
    h2(id="username-heading")
    table(id="profile-table")
    ul(id="interaction-buttons-list")
