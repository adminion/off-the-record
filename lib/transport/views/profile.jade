extends layout
block content
    #contents
        h2 Profile:
        table
            tbody
                each value, key in request.user.toObject()
                    tr
                        td=key
                        td=value
