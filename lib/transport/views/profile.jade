extends layout
block content
    #contents
        h2 Profile:
        table
            tbody
                - var hideKeys = ['salt', 'hash', '_id', '__v', 'privacy']
                each value, key in request.user.toObject()
                    if 0 > hideKeys.indexOf(key)
                        tr
                            td=key
                            td=value
